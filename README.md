# CCSharp8

## Requirements:

- [Docker](https://www.docker.com/get-started)

## Run

Build the container:

```sh
docker build . -t csharp-saludo
```

Run the container:

```sh
docker run -ti csharp-saludo
```
